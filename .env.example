# Copy to `.env` and restart `npx expo start -c`
# Native Maps key is also in app.json → android.config.googleMaps.apiKey
# Optional override for JS (Places autocomplete) — same key as Maps is fine.

EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
