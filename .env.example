# Copy to .env if you use env-based keys. Never commit .env.

# Optional: overrides app.json Google key for Places/Geocoding in JS
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=

# Optional: HTTPS URL to a short WAV/MP3 for job-offer tone (no local asset required for EAS).
# EXPO_PUBLIC_JOB_OFFER_SOUND_URL=
